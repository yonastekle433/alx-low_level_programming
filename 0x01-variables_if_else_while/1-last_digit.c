#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - entry point
 * 
 * Return: always 0
*/

int main(void)
{
              int n;

              srand(time(0));
             n = rand() - RAND_MAX / 2;
                      if ( n > 5 )
                      printf("%s and is greater than %d\n", is, n);
                      if ( n = 0 )
                      printf("%s and is greater than %d\n", is, n);
                      