#include <stdio.h>
#include "main.h"

/**
 * main - entry point
 * 
 * Return: always 0
*/

int _isupper(int c);
{
     if (c >= 'A' && c<= 'Z')
             return (1);
 
     else
          return (0);
}
